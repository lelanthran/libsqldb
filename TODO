====================================================================
Fix the DATETIME handling in the PostgreSQL driver.

====================================================================
Both user/group perms functions that set the perms must return
error if zero rows were affected.

====================================================================
Write a README.md file, maybe a document with a few examples.

====================================================================
Create indices for the various searchable non-TEXT columns in the
schema for sqldb_auth.

====================================================================
Provide a way to query the schema.

====================================================================
Need to differentiate between returned columns that are NULL and
returned columns that are empty strings.

====================================================================
Optimise the sqldb_query module to sort the queries array so that we
can do a binary search when searching for a query.

